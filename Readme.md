# getMicroseconds-chrono
マイクロ秒単位の現在時間を64bitのunsigned intとして返すライブラリです。

``g++ -Wall -o getMicroseconds.dll main.cpp -shared``

``g++ -Wall -o libgetMicroseconds.so main.cpp -shared``

などでコンパイルしてお使いください。